<%if users.any? %>
  <ul class="media-list">
    <% user.each do |user|%>
    <div>
      <%= user.name %>
    </div>
    <div>
      <p><%=link_to 'プロフィール,user_path(user)%></p>
    </div>
    <% end %>
  </ul>
  <%= paginate users %>
<% end %>
